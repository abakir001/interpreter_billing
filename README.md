# interpreter_billing

Create a virtual environment:
```shell
python3 -m venv env
```

Activate environemnt:
```shell
source env/bin/activate
```

Upgrade pip:
```shell
pip install --upgrade pip
```

Install Django
```shell
pip install django
```

